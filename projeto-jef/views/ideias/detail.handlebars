<h2>{{idea.title}}</h2>
<p>{{idea.description}}</p>
<p>Categoria: {{idea.category}}</p>
<p>Votos: 👍 {{upvotes}} | 👎 {{downvotes}}</p>

{{#if (eq idea.owner_id ../user.id) }}
  <a href="/ideias/{{idea.id}}/editar">Editar</a>
  <form method="POST" action="/ideias/{{idea.id}}/deletar" style="display:inline;">
    <input type="hidden" name="_csrf" value="{{csrfToken}}" />
    <button type="submit">Deletar</button>
  </form>
{{/if}}

<form method="POST" action="/ideias/{{idea.id}}/votar">
  <input type="hidden" name="_csrf" value="{{csrfToken}}" />
  <button name="type" value="UP" type="submit">Votar 👍</button>
  <button name="type" value="DOWN" type="submit">Votar 👎</button>
</form>
